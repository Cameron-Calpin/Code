#include <iostream>

using namespace std;

int pow(int x, int y) {
	int count = 0;
	if () {

	}
}

int main() {
	int x, y, result;
	cout << "Enter a number: ";
	cin >> x;
	cout << "Enter a power: ";
	cin >> y;
	result = pow(x, y);
	cout << result;
}